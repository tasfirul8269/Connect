# Frontend environment example for Connections (React)
# Copy to .env and adjust as needed

# Backend API base URL
REACT_APP_API_URL=http://localhost:5000/api

# Google/Facebook OAuth (used by frontend SDKs)
REACT_APP_GOOGLE_CLIENT_ID=
REACT_APP_FACEBOOK_APP_ID=

# Note: Cloudinary uploads use signed parameters from backend, so no Cloudinary
# secrets are needed in the frontend.
